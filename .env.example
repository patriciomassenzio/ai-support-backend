# Optional: leave empty to run in demo mode (rule-based classification)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# SQLite by default; swap for PostgreSQL in production:
# DATABASE_URL=postgresql://user:pass@localhost:5432/support
DATABASE_URL=sqlite:///./support.db
